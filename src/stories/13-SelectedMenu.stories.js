import React from 'react';
import SelectedMenu from '../components/SelectedMenu/SelectedMenu';

export default {
  title: 'SelectedMenu',
  component: SelectedMenu,
};

export const SelectedMenuExample = () => (
  <div style={{ width: '600px', margin: '50px auto' }}>
    <SelectedMenu />
  </div>
);
